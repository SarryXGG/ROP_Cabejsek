Založení dalšího formuláře: Pravé tlačítko na název projektu - Přidat - Nová položka - Formulář Windows. Napsat název formuláře Form2.cs. Nebo můžeme vše naprogramovat viz níže. Modální formulář se otevře pomocí metody ShowDialog(). Hlavní formulář můžeme obsluhovat až po zavření modálního formuláře. Nemodální formulář se otevře pomocí metody Show(). Můžeme obsluhovat oba dva formuláře zároveň.
Form1 je hlavní formulář, který obsahuje tlačítko, které vyvolá druhý modální formulář Form2. Form1 obsahuje komponentu Label, která se nastaví podle textu v komponentě TextBox na formuláři Form2 a naopak.
Form1 je hlavní formulář, který obsahovat dvě tlačítka, které vyvolají druhý nemodální formulář Form2. Form1 obsahuje komponentu Label, která se nastaví podle textu v komponentě TextBox na formuláři Form2 a naopak. Při stisku prvního tlačítka se vždy vytvoří nová instance Form2. Při stisku druhého tlačítka se vždy vytvoří jen jedna instance Form2.
Vytvořte aplikaci, která umožní načíst do ListBoxu 10 slov. Vytvořte modální dialogové okno pro zadávání slova (TextBox), které bude mít tři tlačítka Ulož (zapíše do listBox1) a Zruš (nezapíše) a Přerušit (přeruší zadávání 10-ti slov) Zajistěte, aby políčko pro zadávání textu mělo vždy fokus a při stisku tlačítka Enter se okno zavřelo a slovo se zapsalo do ListBoxu. (textBox1.Clear();this.ActiveControl = textBox1;this.AcceptButton = button1;
Aplikace bude obsahovat hlavní formulář Form1 a další nemodální formulář. Form1 má víceřádkový TextBox a tlačítko, nemodální formulář jen víceřádkový TextBox. Po stisku tlačítka na Form1 se zobrazí nemodální formulář, jenž bude v TextBox obsahovat kopii textu z Form1. Od této chvíle bude text stejný na obou formulářích, ať píšete na kterýkoliv z nich. Zajistěte, aby šla vytvořit jediná instance nemodálního formuláře.
Pomocí modálního formuláře nastavte vzhled tlačítka na hlavním formuláři. Založte vlastní modální formulář, který bude nastavovat text, font, barvu popředí, barvu pozadí, TextAlign, šířku a výšku tlačítka. Dále modální formulář bude obsahovat náhled tlačítka a tlačítko Ulož a Zruš.